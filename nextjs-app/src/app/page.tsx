import ChatWindow from '@/components/ChatWindow';

export default function Home() {
  return (
    <main style={{ height: '100dvh', background: 'var(--bg-primary)' }}>
      <ChatWindow />
    </main>
  );
}
